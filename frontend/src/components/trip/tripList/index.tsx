import React from "react";
import styled from "styled-components";
import { TripListContainer, TripListTitle } from "./styles";


const TripList = () => {
  return (
    <TripListContainer>
      <TripListTitle>서울 식당 목록</TripListTitle>
      
    </TripListContainer>
  )
}

export default TripList
